using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

/// <summary>
/// The PlayerController is a class that handles control of the player game object. Attaching this class
/// to a game object should let the player control the game object - in this case moving in the cardinal
/// directions on a flat plane (Up, Right, Down, Left)
/// </summary>
public class PlayerController : MonoBehaviour
{
    /// <summary>
    /// A built-in Unity function. Start is called before the first frame, and is roughly equivalent to a
    /// "when 🏳 clicked" block.
    /// 
    /// Does not return any value, indicated by `void`.
    /// 
    /// Does not take any parameters.
    /// </summary>
    void Start()
    {
        
    }

    /// <summary>
    /// A built in Unity function. Update is called once per frame, which is 30fps by default. It is roughly
    /// equivalent to a "forever" block inside a "when 🏳 clicked" block.
    /// 
    /// Does not return any value, indicated by `void`.
    /// 
    /// Does not take any parameters.
    /// </summary>
    void Update()
    {

    }
}
